### Example environment variables for StudentApp
# Copy to `.env` and edit values for local development

# MongoDB connection string (change host/user/password as needed)
MONGODB_URI=mongodb://localhost:27017/studentdb

# Backend port
PORT=3000
